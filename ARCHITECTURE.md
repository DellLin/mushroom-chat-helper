# 架構說明

## 資料流

```
[遊戲視窗]
   │ Windows.Graphics.Capture(free-threaded FramePool)
   ▼
capture 執行緒 ── 節流至 N fps ── staging texture → CPU BGRA
   │ FramePacket,bounded(2) channel,滿了丟幀
   ▼
vision 執行緒(每幀依序做下面這幾件事,任何一關沒過就直接丟掉這一幀)
   1. 主畫面判斷:判斷範圍內找不到指定顏色 → 不在遊戲主畫面,整幀跳過
   2. ROI 裁切 + BGRA→RGBA(同一趟掃描完成,裁出來的緩衝區之後直接送給 UI)
   3. 像素標記:逐像素比對頻道色票(核心容差 0.55x);
      亮度快篩門檻依目前啟用頻道的顏色/容差動態算出(不是寫死常數),
      避免深色頻道文字被門檻擋在比對邏輯之前
   4. 整幀快篩:標記雜湊 == 上一幀 → 跳過;在最近 512 筆雜湊裡看過 → 跳過
   5. 主色分類:命中最多的頻道要佔全部命中像素 45% 以上且至少 20 px,
      否則視為未分類 → 跳過
   6. 畫面去重(ImageDedup):把標記依「這則訊息所屬頻道」過濾成二值遮罩,
      跟「前一筆」的遮罩算交集/聯集比例(IoU),同一則訊息因背景抖動重新
      觸發時遮罩幾乎完全重疊,在時間窗內視為重複而略過
   │ ChatImage {channel, time, w, h, rgba}
   ▼
egui UI:聊天檢視(主視窗,逐則把訊息的畫面截圖顯示成材質)+ 設定(獨立視窗,見下)
   ▲
hotkey 執行緒:Win32 RegisterHotKey,系統層級快捷鍵循環切換檢視
```

不做文字辨識(OCR)。屬於哪個頻道完全由「文字顏色」決定,決定後直接把該範圍
的畫面送去顯示,不嘗試理解畫面內容。

vision 執行緒同時負責兩種給 UI 看的畫面:每 500ms 一張縮小的全視窗預覽
(`UiEvent::Preview`,拿來對 ROI 用),以及 UI 主動要求時的一張全解析度原圖
(`UiEvent::FullFrame`,給全螢幕框選/吸色工具用)。這兩者都在主畫面判斷之前
處理,不然使用者一進商城就沒有畫面可以框。

## 執行緒與通道

| 通道 | 型別 | 容量 | 背壓策略 |
|---|---|---|---|
| cmd (UI→capture) | CaptureCmd | unbounded | — |
| frame (capture→vision) | FramePacket | bounded(2) | try_send 丟幀 |
| ui (vision/capture/hotkey→UI) | UiEvent | unbounded | UI 每幀最多汲取 256 筆 |
| hotkey cmd (UI→hotkey) | HotkeyCmd | unbounded | — |

共享狀態:`Arc<RwLock<Config>>`(vision 每幀 clone 一份快照,避免整幀處理期間
握著鎖)、`Arc<AtomicBool>` 全解析度單幀請求(UI 按下「在畫面上框選」/
「吸取畫面顏色」時設一次,vision 讀到後回傳一張 `UiEvent::FullFrame`)、
`Arc<AtomicU64>` 擷取間隔 ms(fps 滑桿即時生效,不必重啟擷取)。

## UI 結構

程式共有三個 egui viewport,都掛在同一個 `egui::Context` 底下,所以樣式
(字級等)設定一次就會同時套用到全部。`ui/` 的檔案切分直接對應這三個 viewport:
`ui/mod.rs` 持有 `App`(狀態 + 事件汲取 + `eframe::App` 實作),`chat.rs`、
`settings.rs`、`picker.rs` 各自用 `impl super::App` 補上自己那個 viewport 的
方法,共用同一份狀態、不必再多一層 context 物件轉手。`update()` 因此只剩
「汲取事件 → 套用字級 → 依序畫三個 viewport」這條主線。

- **主視窗**:無邊框(`with_decorations(false)`)、無系統最小尺寸限制(只留一個
  很小的下限防止縮到抓不到邊框),永遠只渲染聊天內容。每則訊息是一個獨立的
  egui `TextureHandle`(收到 `UiEvent::Message` 時在 UI 執行緒建立),
  以 `MAX_MESSAGES` 為上限避免 GPU 材質數量無限成長。
  拖曳靠工具列背景的自訂 drag sense 區(`ViewportCommand::StartDrag`),
  縮放靠手動裝的邊框/角落感應區(`ViewportCommand::BeginResize`,只留
  右/下/右下角)。這兩個都可以被「鎖定」暫時停用。
- **設定視窗**:按 ⚙ 用 `show_viewport_immediate` 開出的獨立 OS 視窗,保留系統
  裝飾(標題列/關閉鈕),內含四個分頁(擷取設定/頻道/檢視管理/說明)跟
  「結束應用程式」按鈕——這是唯一能完全關閉程式的地方,因為主視窗沒有關閉鈕。
  再按一次 ⚙ 若視窗已開著,會送 `ViewportCommand::Focus` 把它叫到前面,而不是
  重新開一個。
- **全螢幕擷取工具**:框選 ROI 與吸取顏色共用的全螢幕置頂疊圖視窗
  (`ui_screen_picker`),顯示剛擷取到的全解析度原圖,隱藏系統游標改畫十字準心
  加逐像素放大鏡。兩種操作只有點擊語意不同(框選要點兩下決定起點/終點,吸色
  點一下就套用),座標換算與顯示走同一套 `to_frame`/`to_screen`。

「全部」檢視的行為由 `Config::all_view_mode` 決定:
- `ShowMessages`:正常顯示未過濾的訊息截圖列表。
- `HideChat`(預設):把主視窗**實際縮小**到只剩工具列高度(不是用半透明遮罩
  蓋掉),讓底下的遊戲聊天視窗直接露出來;切回其他檢視時還原成收合前的高度。
  用視窗大小而非透明合成實現,行為比依賴 Windows 桌面合成的透明效果穩定。

**視窗位置/大小記憶**:`update()` 每幀讀 `ctx.input(|i| i.viewport().inner_rect)`,
跟 `Config::window_pos`/`window_size` 不同就寫回(先用 read lock 比較,真的
有變動才升級成 write lock,避免拖曳視窗時每幀都搶鎖)。收合成只剩工具列高度
時故意不寫回 `window_size`、被主畫面判斷搬到螢幕外時故意不寫回 `window_pos`,
不然下次啟動視窗會直接生成在收合高度或螢幕外面。`main.rs` 啟動時讀這兩個欄位
建立初始 `ViewportBuilder`(`with_position`/`with_inner_size`);沒有記錄過就
退回寫死的預設值跟作業系統決定的位置。

位置另外要過 `desktop::pos_is_reachable`(視窗左上角有沒有落在虛擬桌面內)這關,
寫回設定檔前擋一次、啟動還原時擋一次、從自動隱藏搬回來時再擋一次。光靠上面那個
`chat_hidden_by_gate` 旗標不夠:送出「搬回原位」的那一幀,`inner_rect` 讀到的還是
搬走前的螢幕外座標(視窗命令下一幀才生效),照寫就會把 `-32000` 存進設定檔,
下次啟動視窗直接生在看不見的地方。第一幀還會由 `ui/chat.rs` 的
`ensure_window_reachable` 用實際的 `pixels_per_point` 再精準檢查一次,不合格就
直接搬回桌面左上角——設定檔可能是舊版本存壞的,上次用的那台螢幕也可能拔掉了。

**「視窗置頂」同步到設定視窗**:設定視窗的 `ViewportBuilder` 每幀都用目前的
`always_on_top` 重建(`with_window_level`),不需要自己額外判斷「值有沒有變」
——egui 內部的 `ViewportBuilder::patch` 本來就會跟上一幀的 builder 比較,
只有真的改變時才送出 `ViewportCommand::WindowLevel`,所以每幀重建不會造成
額外開銷或閃爍。

## 主畫面判斷(gate)

`Config::gate` 定義一個小範圍與一個目標顏色:該範圍內有足夠多像素命中目標顏色
就視為「目前在遊戲主畫面」。vision 執行緒每幀先做這個判斷,不在主畫面(商城、
拍賣等畫面)時整幀跳過,狀態改變時送一次 `UiEvent::GateState`。

UI 收到 `false` 且使用者有開「自動隱藏」時,會把主視窗整個搬到螢幕座標範圍外
(`OuterPosition(-32000, -32000)`),回到主畫面時再搬回原位。刻意不用
`ViewportCommand::Visible` 隱藏視窗——實測隱藏後 winit 不會再收到重繪機會,
視窗就再也叫不回來了。

### 自動隱藏一定要解得開

「搬到螢幕外」這招的代價是:只要 `true` 那一次沒送出去,視窗就永遠留在外面。
`false` 是很容易變成永久狀態的——判斷顏色沒校正好、換了解析度、遊戲關掉之後
根本不會再有畫面可以判斷。所以隱藏這條路上疊了四道各自獨立的保險:

| 保險 | 位置 | 擋掉的情況 |
|---|---|---|
| 判斷要先自己證明過一次 | `vision::GateReporter` | 判斷顏色/範圍沒校正好,`false` 從頭到尾不會翻身。至少真的看過一次「在主畫面」,之後的 `false` 才算數;沒看過就一律回報 `true`(該跳過的畫面照樣跳過,只是不隱藏視窗) |
| 畫面斷流看門狗 | `vision`(`recv_timeout`,見 `FRAME_IDLE_TIMEOUT`) | 擷取的遊戲關掉/最小化/擷取卡住——沒有畫面就等不到下一次狀態改變。超時就補送一次 `true`,並忘掉確認狀態,畫面回來時重新確認 |
| 擷取目標關閉事件 | `capture/wgc.rs`(`GraphicsCaptureItem::Closed`) | WGC 在目標視窗關閉時只是安靜地不再送畫面、不回報錯誤。收到事件就自己下 `CaptureCmd::Stop`,UI 收到非 `Running` 狀態就把 `gate_open` 強制設回 `true` |
| 啟動寬限期 | `ui/chat.rs`(`GATE_STARTUP_GRACE`) | 上面三道都失守時的最後退路:剛啟動的前幾秒一律不隱藏,「關掉重開」永遠看得到視窗 |

再加上視窗位置本身的保護(見上一節與 `desktop.rs`):螢幕外的座標永遠不會被
寫回設定檔,啟動時也會再檢查一次視窗是不是真的生在桌面上。

## ROI 設定:一般 / 進階

`Config::roi_mode` 有兩種模式:

- `Preset(ResolutionPreset)`(一般設定):直接套用該解析度下**實測**的聊天 ROI
  與主畫面判斷 ROI(`config.rs` 裡的 `CHAT_ROI_*`/`GATE_ROI_*` 常數)。不是用
  比例換算——不同解析度下遊戲 UI 不一定照比例縮放,實測才準。收到 `Preview`
  事件時會依實際擷取到的畫面大小自動切換到對應的解析度選項。
- `Advanced`(進階設定):手動輸入座標,或在預覽圖上拖曳框選 / 用全螢幕工具框選。

進階模式輸入的值每幀另外存進 `custom_roi`/`custom_gate_roi`,跟一般設定換算出來
的 `roi`/`gate.roi` 分開放,這樣「切到一般設定再切回進階設定」時使用者自己輸入
的座標還在,不會被覆蓋掉。

## 關鍵設計決策

**為什麼放棄 OCR,改成直接顯示畫面截圖?**
Windows.Media.Ocr 對楓之谷這類小字、半透明背景聊天字型的辨識率不夠穩定,
常見誤字、漏字。與其花大量前處理(遮罩、裁切、放大、膨脹、模糊)換取仍不
可靠的文字,不如放棄「理解」畫面內容,只做「文字顏色 → 頻道」的分類,
分類完直接把該範圍的原始畫面送去顯示。代價是不能搜尋文字、也無法用文字內容
做精確去重,換來的是準確、零誤字的呈現。

**為什麼沒有「切行」邏輯,ROI 本身就是一則訊息?**
早期版本用「有文字像素的列 vs 空白列」找行的邊界,但半透明背景疊在會動的
遊戲畫面上,容易讓相鄰行之間該有的空白間隔消失,導致多行被誤判黏成一個
巨大的區塊;後來改成固定高度網格從 ROI 頂端往下切,但這樣使用者要手動調整
「切行高度」跟 ROI 高度對齊,兩個數字容易脫節,調校體驗差。既然使用慣例本來
就是 ROI 只框住單一則對話,乾脆讓 ROI 高度直接定義處理範圍,把切行這個容易
搞混、也不再需要獨立存在的概念整個砍掉。同一時間最多只有一則訊息,不會有
「同一則訊息隨畫面捲動移進不同格子」的問題,畫面去重(見下)只需要跟
「前一筆」比對就夠。

**為什麼要有畫面去重(ImageDedup)?**
放棄 OCR 後失去了文字時代靠「OCR 文字模糊比對」擋掉的重複來源:半透明背景
抖動讓分類標記每隔一段時間就被判定為「變了」,同一則訊息因此重新觸發送出。
這種情況下訊息的視覺內容其實幾乎沒變,只是有雜訊,所以改用內容感知的遮罩
IoU 比對來擋,而不是單純按時間節流(按時間節流連內容不同的連續訊息也會
一起擋掉)。

**為什麼要比對「分類遮罩」,不能直接對原始畫面(rgba)比?**
第一版直接對送去顯示的原始畫面(含背景)比對,結果玩家移動、特效閃爍
造成背景劇烈變化時,同一則靜止不動的文字每一幀看起來都差很多,去重
完全失效、瘋狂重複——半透明聊天背景本來就會透出後面持續變動的遊戲畫面,
拿它來比對等於拿雜訊當內容比。改成只看「這個像素有沒有命中頻道文字
顏色」的二元分類遮罩(和 `label_pixels` 用同一份分類結果)。這樣背景不管
怎麼動都不影響判斷,只有文字本身真的改變才會被當成新訊息。

**為什麼 `channel_mask_bits` 只保留「這則訊息所屬頻道」的顏色,不是「任何頻道
都算命中」?**
同一個 ROI 位置偶爾會被其他頻道的文字短暫覆蓋或殘留雜色(例如捲動瞬間、
半透明背景疊到別的顏色),如果遮罩不分頻道地把「任何命中」都算進來,
這些跟目前這則訊息無關的雜色也會混進比對範圍,稀釋掉真正決定訊息內容的
訊號。只保留目前分類到的那個頻道,比對範圍就精準對應「這則訊息的文字本身」。

**為什麼要保留完整 2D 遮罩,不再壓成一維水平投影?**
早期為了不受「捲動到新格子造成的垂直對齊誤差」影響,把整個處理範圍的高度壓成
一維(只看每欄的命中比例),但這樣會漏失文字的縱向筆劃形狀——短訊息、或
同一人連續講字數相同但內容不同的話時,兩則訊息的「每欄命中比例」剛好相近
是很常見的情況(尤其中文字寬度接近、排列方式相似),一維投影分辨不出來,
容易誤判成同一則而漏顯示。現在 ROI 固定框住同一個螢幕位置、不會再有捲動換格,
同一畫面重複擷取時垂直對齊本來就很穩定,不需要犧牲 y 軸解析度換取容錯,改成
保留完整 2D 遮罩(`channel_mask_bits`,打包成 bit 陣列),用交集/聯集比例
(IoU,`mask_similarity`)比對,同一則訊息重疊比例通常 >0.9,不同內容
(即使命中面積相近)重疊比例通常很低,兩者區隔明顯。IoU 是比例而非絕對
像素差異數,短訊息命中像素少也不會被大片背景稀釋掉,能公平反映「內容
像不像」而不受訊息長短影響。

**為什麼 ImageDedup 只跟「前一筆」比對,不維護時間窗內的歷史清單?**
早期版本要同時處理「一個 ROI 裡有好幾行」以及「同一則訊息隨捲動換到不同
格子」,所以拿時間窗內看過的所有訊息、甚至跨鄰近格子模糊比對。現在 ROI
慣例只框住單一則對話,每次擷取範圍裡最多一則訊息,問題簡化成「這次跟
上一次是不是同一則」,不再需要維護歷史清單或依格子位置分桶比對。

**為什麼命中像素太少時要整個跳過去重(MIN_DEDUP_PIXELS)?**
極短訊息或邊緣雜訊命中的像素數很少,逐幀出現的雜訊點位不穩定,持續拿去
跟前一筆比對容易一直判定成「新訊息」而洗版,乾脆在命中像素數低於
`MIN_DEDUP_PIXELS` 時整個跳過比對,一律放行,避免長期把零星雜訊也當成
訊息串流處理。

**為什麼亮度快篩門檻要動態算,不能寫死?**
早期版本寫死 `r.max(g).max(b) < 90` 就丟棄,理由是快速濾掉背景雜訊像素。
但如果使用者設定了較暗的頻道顏色(例如深酒紅色),這個門檻會在容差比對
邏輯跑之前就把合法的文字像素擋掉,不管容差調多寬都救不回來,而且錯誤現象
很難跟「顏色/容差沒調準」區分。改成依目前啟用頻道的顏色與容差動態算出最
寬鬆的合理門檻,對深色頻道更友善,同時仍保留快篩背景雜訊的效果。

**為什麼先分類再送出畫面?**
顏色分類只需一次整數比較掃描,成本遠低於無條件送出截圖(材質建立與 GPU 上傳
都有成本)。未分類的畫面直接跳過,省下大量算力與記憶體;未分類代表沒有任何
頻道文字顏色明確命中,一律不顯示。

**為什麼顏色可調整而非寫死?**
遊戲改版、UI 透明度、不同語系都會偏移色值。頻道分頁每個色票旁的
「🎯 吸取畫面顏色」會開一個全螢幕畫面(跟框選 ROI 共用同一套機制),點一下
指定像素就直接把該處的 RGB 套用到這個頻道,比任何硬編碼耐用。

**為什麼系統層級快捷鍵用 RegisterHotKey 而不是 egui 內部按鍵監聽?**
egui 的鍵盤事件只在應用程式視窗有焦點時才收得到,但這個工具的典型使用情境
是遊戲視窗在前景、應用程式視窗在背景疊圖。Win32 `RegisterHotKey` 註冊的是
系統層級的全域快捷鍵,不論哪個視窗有焦點都能觸發,獨立執行緒輪詢
`WM_HOTKEY` 訊息,搭配一個小命令 channel 讓使用者可以即時更換組合鍵
(先 `UnregisterHotKey` 舊的、再 `RegisterHotKey` 新的)。

**為什麼純 Rust 影像處理?**
需要的操作(容差比對、裁切、色彩轉換、位元運算)都是幾十行迴圈,換 OpenCV 的
代價是 Windows 上出名難搞的建置與 DLL 發佈。

**擷取細節**
- FramePool 用 `CreateFreeThreaded`,回呼在 MTA 執行緒,不需訊息迴圈;
  `HandlerCtx` 的 COM 欄位被 `unsafe impl Send/Sync`,因為執行緒已用
  `RO_INIT_MULTITHREADED` 初始化,free-threaded pool 本來就允許回呼在
  任意執行緒觸發
- 回呼內先 `TryGetNextFrame` 取走幀再節流,避免 pool 積壓
- `ContentSize` 變動時 `Recreate` pool(視窗縮放)
- staging texture 快取重用,只在尺寸變動時重建
- D3D11 裝置優先建立硬體裝置,失敗才退回 WARP 軟體算繪

## 模組對應

| 路徑 | 職責 |
|---|---|
| `main.rs` | 執行緒編排、channel 建立、主視窗初始化 |
| `model.rs` | 執行緒間的訊息/指令型別 |
| `config.rs` | TOML 設定持久化、各解析度實測 ROI 常數 |
| `desktop.rs` | 虛擬桌面範圍查詢、「這個視窗位置還叫得回來嗎」判斷 |
| `capture/wgc.rs` | WGC 擷取執行緒、D3D11 讀回 |
| `capture/win_enum.rs` | 可見視窗列舉 |
| `vision/mod.rs` | 管線編排:主畫面判斷 → ROI 裁切 → 標記 → 分類 → 去重 → 送出畫面;主畫面判斷的回報規則(`GateReporter`)與畫面斷流看門狗 |
| `vision/classify.rs` | 色票(含動態亮度門檻)、像素標記、主色統計、頻道遮罩與 IoU 比對 |
| `vision/dedup.rs` | 標記雜湊 LRU / 畫面去重時間窗(ImageDedup) |
| `hotkey.rs` | 系統層級全域快捷鍵(RegisterHotKey)執行緒 |
| `ui/mod.rs` | `App` 狀態、UiEvent 汲取、`eframe::App` 實作(三個 viewport 的編排) |
| `ui/chat.rs` | 主視窗:工具列、訊息截圖列表、收合/拖曳/縮放/自動隱藏 |
| `ui/settings.rs` | 設定視窗與四個分頁(擷取設定/頻道/檢視管理/說明) |
| `ui/picker.rs` | 全螢幕框選 ROI / 吸取顏色工具,以及觸發它的色票按鈕 |
